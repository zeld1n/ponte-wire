apiVersion: v1
kind: Service
metadata:
  name: pw-gateway
spec:
  selector:
    app: pw-gateway
  ports:
    - protocol: TCP
      port: 8080
      targetPort: {{ .Values.gateway.port }}
  type: ClusterIP